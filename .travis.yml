language: node_js
node_js:
  - 'stable'

env:
  - API_VERSION=v1

install: 
  - npm install

script:
  - 'true'

after_script:
  - npm run coveralls
  - npm run coverage
  - ./cc-test-reporter after-build -t lcov --exit-code $TRAVIS_TEST_RESULT
  
cache:
  directories:
  - "$HOME/.npm"
